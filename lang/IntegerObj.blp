pack IntegerObj {

  import pack::Object;
  import pack::StringObj;

  obj IntegerObj: pub Object{
    priv Integer thisInt;

    pub con(int: Integer) => void;
    pub getUnderlyingInteger() => Integer;
    pub asString() => StringObj;
    pub decon() => void;
  };

};